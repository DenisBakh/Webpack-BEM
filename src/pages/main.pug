include ../project/common.blocks/_common/utils/mixins
block variables

doctype html
html(lang='ru')
  head
    meta(charset='utf-8')
    title #{title}
    meta(name="description", content=description)
    block head

  body(class=pageClass)
    .wrapper
      #app
        include ../project/common.blocks/header/header.pug
        .content-wrapper
          block content
        include ../project/common.blocks/footer/footer.pug

    block script